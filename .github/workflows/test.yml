name: test

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: |
          chmod +x ./scripts/test.sh"
          ./scripts/test.sh
